#import "/_lib/wanshi.typ": *

#show: wanshi

#metadata((
  "title": "References",
  "taxon": "collection",
  "collect": "true",
  // A hub that links every work would otherwise put a backlink to itself on
  // each one, burying the backlinks that matter — the notes actually citing it.
  "asback": "false",
))

Works cited anywhere in these notes. Each page is generated from my Zotero
library by `wanshi refs sync`, and lists the notes that cite it.

#by-taxon("reference", sort: "date", order: "desc", title: none)
