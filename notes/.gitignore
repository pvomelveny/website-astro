# Generated by wanshi
/.cache
.DS_Store

# Build output only lands here if wanshi is run with the wrong working
# directory — `wanshi build --config notes/Wanshi.toml` from the repo root
# resolves `[build].output` inconsistently. Always `cd notes` first; the npm
# scripts do. Ignored so a stray run cannot be committed by accident.
/public/
