# Generated by wanshi
/.cache
.DS_Store

# Kept as a guard. Output used to land here when wanshi was run from outside
# the project: `output_path` applied the project root twice, so pages went to
# `notes/notes/../public/notes`. Fixed in wanshi on 2026-08-17, but an older
# binary would still do it, and a stray build should not be committable.
/public/
